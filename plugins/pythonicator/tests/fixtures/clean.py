"""A conforming sample module."""


def add_two_numbers(first: int, second: int) -> int:
    """Return the sum of two integers.

    Args:
        first: The first addend.
        second: The second addend.

    Returns:
        The sum.
    """
    return first + second
